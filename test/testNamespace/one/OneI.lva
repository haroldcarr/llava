(package testNamespace.one OneI)

(import testNamespace.two.TwoI)

(letrec ((minT (lambda args (minAux (car args) (cdr args))))
	 (minAux (lambda (smallest args)
		   (if (null? args)
		       smallest
		       (minAux (if (< (car args) smallest)
				   (car args)
				   smallest)
			       (cdr args))))))
  (set! min minT))

;; End of file.
